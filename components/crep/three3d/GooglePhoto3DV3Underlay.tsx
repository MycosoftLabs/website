"use client"

/**
 * RECONSTRUCTED dormant stub (see lib/geo/crep-map-fly-to.ts header). The v3 Google
 * photorealistic 3D underlay — Cursor's lane. Renders nothing (no mock data) until the real
 * component is synced; the legacy globe never mounts it (gated behind v3Photo3dOn in
 * CREPDashboardClient). Cursor's real version supersedes on sync.
 */
export default function GooglePhoto3DV3Underlay(_props: { enabled?: boolean }): null {
  return null
}
