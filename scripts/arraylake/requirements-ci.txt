# Reproducible dependency set for .github/workflows/arraylake-field-bake.yml.
# Update deliberately after an offline compatibility check; do not let the scheduled
# job silently absorb new Arraylake/Zarr/Xarray behavior.
arraylake==1.3.0
icechunk==2.1.2
numpy==2.5.2
pillow==12.3.0
pyproj==3.7.2
rioxarray==0.23.0
ruamel-yaml==0.19.1
xarray==2026.7.0
zarr==3.3.0
