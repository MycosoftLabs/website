# Website CI — lint + production build (May 02, 2026)
# Sourcery AI GitHub App (sourcery-ai[bot]) already reviews MycosoftLabs PRs.
# Do not add sourcery-ai/action@v1 here: repo secret SOURCERY_TOKEN is not set.
name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: website-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: Install
        run: npm ci

      - name: ESLint
        run: npm run lint
        # Warnings are allowed; fix in codebase separately.

      - name: Add swap for Next.js heap
        run: |
          sudo swapoff -a || true
          sudo fallocate -l 8G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile

      - name: Production build
        env:
          NODE_OPTIONS: --max-old-space-size=8192
        run: npm run build
