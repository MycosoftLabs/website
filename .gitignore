.next/
node_modules/
dist/
out/
.turbo/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# OS / editor
.DS_Store
Thumbs.db

# Cursor / IDE
.cursor/
.vscode/

# Environment
.env
.env.*
.env.local
.env.development.local
.env.test.local
.env.production.local
.credentials.local

# TypeScript
*.tsbuildinfo

# Playwright (May 03 2026)
playwright-report/
test-results/
e2e/widgets/_artifacts/*.png

# Misc
coverage/
tmp/
temp/
node_modules

# Local data/cache (do not commit)
.data/
data/
var/cache/
var/logs/
# BUT: public/data/ contains real bundled infrastructure GeoJSON that
# the client serves as static assets (submarine cables, military bases,
# airport perimeters, etc.). These files are committed by design.
!public/data/
!public/data/**
__pycache__/
videos/
Mushroom1/
Sporebase/
/keys/

# NAS-backed media (handled on sandbox via mount)
public/assets/
agaric/**/*.mp4
agaric/**/*.mov
agaric/**/*.webm
credentials.local

# Worldview launch private artifacts (May 22, 2026)
docs/launch/2026-05-22/fixtures/api_keys.env.PRIVATE
docs/launch/2026-05-22/harness/reports/

# CUI audit JUL15 2026 ? build/temp hygiene
temp_original_crep.txt
tsc_output_utf8.txt
*.tsc_output*.txt

# Psathyrella operator-correction capture (training data + frame crops from the live buoy).
# Local-only: may contain imagery of the deployment environment. Never commit.
.psathyrella/

# Private / program-sensitive local docs (public GitHub repo)
# See MAS docs/PUBLIC_REPO_DOCS_HYGIENE_AUG03_2026.md
docs/private/**
!docs/private/README.md

# Launchpad synthetic demo account credentials (local only)
.launchpad-demo.local

# Launchpad MYCA local harness (BYO AI keys + lp_ tokens — never commit)
.launchpad-myca.local.json
.launchpad-myca.local.yaml
**/launchpad-myca.local.json
services/launchpad-myca-harness/.venv/
services/launchpad-myca-harness/inbox/
services/launchpad-myca-harness/data/
