.next/
node_modules/
dist/
out/
.turbo/

# Build/audit/operational debris — must never be committed (see SECURITY_AUDIT_2026-06-09.md)
git_diff*.txt
git-status.txt
conflict.diff
pr_logs.txt
get_pr_logs.js
fix.js
node-processes.txt
cmd.json
x402.txt
tsout.txt
tsc_output*.txt
ts_errors.txt
temp_original_*.txt
site-media-audit*.json
asset-audit-*.json
asset-audit-*.tsv
scripts/_*_check_result.txt

# Employee PII — provision out-of-band on the VM at /opt/mycosoft/config/security/
config/security/authorized-users.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# OS / editor
.DS_Store
Thumbs.db

# Cursor / IDE
.cursor/
.vscode/

# Environment
.env
.env.*
.env.local
.env.development.local
.env.test.local
.env.production.local
.credentials.local

# TypeScript
*.tsbuildinfo

# Playwright (May 03 2026)
playwright-report/
test-results/
e2e/widgets/_artifacts/*.png

# Misc
coverage/
tmp/
temp/
node_modules

# Local data/cache (do not commit)
.data/
data/
var/cache/
var/logs/
# BUT: public/data/ contains real bundled infrastructure GeoJSON that
# the client serves as static assets (submarine cables, military bases,
# airport perimeters, etc.). These files are committed by design.
!public/data/
!public/data/**
__pycache__/
videos/
Mushroom1/
Sporebase/
/keys/

# NAS-backed media (handled on sandbox via mount)
public/assets/
agaric/**/*.mp4
agaric/**/*.mov
agaric/**/*.webm
credentials.local

# Worldview launch private artifacts (May 22, 2026)
docs/launch/2026-05-22/fixtures/api_keys.env.PRIVATE
docs/launch/2026-05-22/harness/reports/
